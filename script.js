function ToggleMode() {
   const html = document.documentElement
   // if(html.classList.contains('light')) {
   // html.classList.remove('light') }
   // else {
   // html.classList.add('light') }

   html.classList.toggle('light') //se tiver o light ele tira se não coloca
}
   